package com.itineraryledger.kabengosafaris.EmailAccount.EmailMessage.ModalEntity;

/**
 * Constrained label palette. Each value maps to a `--label-{value}` CSS
 * variable pair on the frontend (see kabengo-safaris-management
 * theme.css and EMAIL_INBOX_API.md §1).
 */
public enum EmailLabelColor {
    QUOTE,
    BOOKING,
    VENDOR,
    INTERNAL
}
